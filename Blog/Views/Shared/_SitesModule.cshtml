@model List<Blog.ViewModels.SitesModuleViewModel>

<ul>
    <li><a href="/">Strona główna</a></li>
    @for(int i=0; i<Model.Count; i++)
    {
        <li>@Html.ActionLink(Model[i].Title, "Site", "Sites", new{id = Model[i].Alias}, null)</li>
    }
</ul>