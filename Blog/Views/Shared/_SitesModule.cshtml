@model List<Blog.ViewModels.SitesModuleViewModel>

<ul>
    @for(int i=0; i<Model.Count; i++)
    {
        <li>@Html.ActionLink(Model[i].Title, "Site", "Sites", new{id = Model[i].Alias}, new{@class = "btn btn-primary"})</li>
    }
</ul>